import React from "react"
import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'

import './globals.css'

const _geist = Geist({ subsets: ['latin'] })
const _geistMono = Geist_Mono({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'AESTHETE - Cinematic Art Transition',
  description: 'Cinematic art transition with scroll-driven video frame-by-frame playback',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className="font-sans antialiased m-0 p-0 overflow-hidden bg-black">{children}</body>
    </html>
  )
}
